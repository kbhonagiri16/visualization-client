go_import_path: github.com/kbhonagiri16/visualization-client

language: go

go:
  - 1.7.x

os:
  - linux

before_install:
  - make init

script:
  - make stylecheck
  - make lint
  - make test
  - make build-all
  - make test-integration
  - make package-init
  - make package
